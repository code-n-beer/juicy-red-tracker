(ns re-frame-pomofront.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [re-frame-pomofront.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
