'use strict';

import React from 'react';

require('styles/test/Stateless.css');

let StatelessComponent = (props) => (
  <div className="stateless-component">
    Please edit src/components/test//StatelessComponent.js to update this component!
  </div>
);

StatelessComponent.displayName = 'TestStatelessComponent';

// Uncomment properties you need
// StatelessComponent.propTypes = {};
// StatelessComponent.defaultProps = {};

export default StatelessComponent;
